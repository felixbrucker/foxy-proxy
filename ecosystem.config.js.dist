module.exports = {
  apps: [
    {
      name: 'burst-proxy',
      script: './main.js',
      watch: true,
      ignore_watch: ['db', 'node_modules', '.git'],
      env: {
        DATABASE_URL: 'sqlite:db/database.sqlite',
      },
    }
  ]
};
